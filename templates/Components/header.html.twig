<nav class="navbar navbar-expand-lg navbar-dark bg-dark " id="header">
    <div class="nav__wrap d-flex align-items-center justify-content-between flex-wrap">
        <a class="navbar-brand h1 mb-0" href="/" id="brand__name"><b>Ox</b>Dictionary</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
      
         {% set links = {'Home':"/","Login":"/login","Sign up":"/signup"}%}
          
          <ul class="navbar-nav mr-auto">

          {% for linkName,href in links %}
            <li class="nav-item  ">
              <a class="nav-link" href="{{href}}">{{linkName}}</a>
            </li>
          {% endfor %}
                   
          </ul>
          <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-danger my-2 my-sm-0" type="submit">Search</button>
          </form>
        </div>
    </div>
  </nav>

 